# 文稿写作类交付前核查清单

用途：文稿、报告、公文、讲话稿等写作类交付物提交前逐项对照。每项判定结果只有「通过」或「未通过」两种；未通过项修复后复检，无法修复的如实说明。验证方式分四类：读检（重读文本确认）、检源（核对原始来源）、运行（实际执行验证）、比对（与标准或样本比对）。

## 一、清单

### 1.1 结构与格式

1. 标题编号：二级标题用「一、」配 `##`，三级标题用「1.1」配 `###`；不存在四级及以下 Markdown 标题。验证方式：读检。
2. 标题承载：每个标题下自然段数不少于三段；不足三段的内容并入相邻章节；标题数量宁少勿多。验证方式：读检。
3. 摘要与总结：标题之后、正文之前有摘要；复杂任务末尾有归纳且未与摘要重复表述。验证方式：读检。
4. 引号场景：研究报告场景用直角双引号「」；公文场景用常规弯角引号；全文一致。验证方式：读检。
5. 禁用标记：全文无 Markdown 加粗标记与分割线标记。验证方式：读检。
6. 行内代码：代码、命令、文件路径、配置键、HTML 或 XML 标签及含 `<`、`>`、`*`、`_` 等保留字符的字面文本已用反引号包裹；普通中文术语未误标；多行代码使用三反引号代码块并标注语言。验证方式：读检。
7. 段落形态：以段落论述为主；列表仅在字数少于30字且并列关系明确时出现；无一句一段的碎片化拼接。验证方式：读检。
8. 中文排版：数字用法、标点符号、中英文混排空格符合 `typography.md` 〔强制〕级条款；公文场景符合 GB9704 相关条款（纸张、字体字号、成文日期、结构层次序数）。验证方式：读检。
9. 文档元数据：落盘 .md 文件顶部含仅有一个 date 键的 YAML frontmatter，日期为文档创建当天；修订既有文档时 date 保留原创建日期；对话内输出的长文与公文对外终稿不含 frontmatter。验证方式：读检。

### 1.2 内容与论证

1. 金字塔结构：首段给出核心结论，其后为依据、机制、条件和限制。验证方式：读检。
2. 事实核验：事实性内容已经豆包搜索与内置搜索交叉验证；引用来源真实存在且链接可访问；未编造数据、出处与引文；无法核验处已标注不确定性。验证方式：检源。

### 1.3 交付说明

1. 已验证项列表：交付时向用户列出已验证项及对应验证方式（读检、检源、运行、比对）。
2. 未验证项说明：存在未验证项时，如实说明原因；不得谎报已验证。

## 二、输出格式模板

交付时在文稿之后附以下格式的验证说明：

1. 已验证项：逐项列出清单编号、验证方式、结果。
2. 未验证项：逐项列出编号、未验证原因、建议的后续验证方式。

## 三、其他任务类型的清单

1. 代码类：逐项运行测试命令，记录运行结果。
2. 界面类：逐屏截图比对，记录比对结论。
3. 数据分析类：复核表格与图表数据一致性，记录复核范围。

（上述三类不属于本清单范围，按自定义指令「交付前自检」条款执行。）
